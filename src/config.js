import { version } from "react";

const config = {
  apiBaseUrl: 'https://retos-back.vercel.app',
/*  apiBaseUrl: window.location.hostname === 'localhost'
    ? 'http://localhost:5000'
    : 'http://192.168.100.35:5000', 
  version: "1.8"
*/
};
  


  export default config;
    